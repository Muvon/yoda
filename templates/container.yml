image: base
hostname: ${HOSTNAME}.${COMPOSE_PROJECT_NAME}.{{name}}.#
labels:
  - role={{name}}
working_dir: /src
entrypoint: /src/docker/containers/{{name}}/entrypoint
network_mode: bridge
restart: unless-stopped
cap_drop:
  - ALL
